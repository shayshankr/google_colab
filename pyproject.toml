[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hvac-e20-heat-load-tool"
version = "0.1.0"
description = "E20-style HVAC heat load calculation tool"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["hvac_e20_heat_load_tool"]
